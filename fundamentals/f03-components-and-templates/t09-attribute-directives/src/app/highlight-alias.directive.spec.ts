import { HighlightAliasDirective } from './highlight-alias.directive';

describe('HighlightAliasDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightAliasDirective();
    expect(directive).toBeTruthy();
  });
});
